# Copy this to .env.local and fill in your values
# Never commit .env or .env.local to git

# Google Maps API (for address autocomplete)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyCpm-vlpj8MFo8MpIOBEg41FTXbELe9Bgg

# Database (PostgreSQL - use Neon, Supabase, or local)
# Example: postgresql://user:password@localhost:5432/mhr
DATABASE_URL=postgresql://user:password@host:5432/database

# NextAuth (generate with: openssl rand -base64 32)
AUTH_SECRET=your-secret-at-least-32-chars
# Required for auth callbacks (use your app URL: http://localhost:3000 for local)
NEXTAUTH_URL=http://localhost:3000

# Public site URL (for sitemap, robots.txt, metadata - use https://myhockeyrecruiting.com in production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Twilio (for SMS verification)
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+15551234567

# Twilio Verify (recommended - uses pre-approved templates)
# Create at: https://console.twilio.com/us1/develop/verify/services
TWILIO_VERIFY_SERVICE_SID=VAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe (for subscriptions)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_GOLD_MONTHLY=price_...
STRIPE_PRICE_GOLD_ANNUAL=price_...
STRIPE_PRICE_ELITE_MONTHLY=price_...
STRIPE_PRICE_ELITE_ANNUAL=price_...
STRIPE_PRICE_FAMILY_GOLD_MONTHLY=price_...
STRIPE_PRICE_FAMILY_GOLD_ANNUAL=price_...
STRIPE_PRICE_FAMILY_ELITE_MONTHLY=price_...
STRIPE_PRICE_FAMILY_ELITE_ANNUAL=price_...

# Cron (for Vercel cron jobs - event reminders, etc.)
# Generate with: openssl rand -hex 32; add to Vercel env as CRON_SECRET
CRON_SECRET=

# Admin / Zapier
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=
# Zapier Catch Hook URL - when set, new contact messages, disputes, and training submissions are posted here
ZAPIER_WEBHOOK_URL=

# Change demo/admin passwords (scripts/change-demo-passwords.ts)
# Add temporarily, run npm run change-demo-passwords, then remove
# NEW_DEMO_PASSWORD=

# Vercel API (for scripts/sync-env-to-vercel.ts — create at https://vercel.com/account/tokens)
# VERCEL_TOKEN=

# Optional
PORT=3000
